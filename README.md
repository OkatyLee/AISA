# AI Scientific Assistant (AISA)

**AI Scientific Assistant** — это продвинутый Telegram-бот с интегрированным Mini App, предназначенный для поиска, агрегации и анализа научных статей. Ассистент помогает исследователям эффективно работать с публикациями из различных источников (arXiv, IEEE, PubMed, Semantic Scholar), управлять личной библиотекой и получать интеллектуальные рекомендации.

## 🚀 Основные возможности

*   **🔍 Мульти-поиск**: Единый интерфейс для поиска по базам arXiv, IEEE Xplore, NCBI (PubMed) и Semantic Scholar.
*   **📚 Личная библиотека**: Сохранение найденных статей, добавление тегов и заметок. Управление коллекцией через Telegram или веб-интерфейс Mini App.
*   **📱 Telegram Mini App**: Полноценное веб-приложение внутри Telegram для удобного просмотра библиотеки и чтения статей.
*   **🧠 NLU & AI (Unified Pipeline)**:
    *   **Локальная LLM (Ollama)**: Классификация намерений и извлечение сущностей (topic, author, year) работают локально на `qwen2.5:3b`.
    *   **Умный роутинг**: Обычное общение — в Ollama, тяжёлые задачи (суммаризация, сравнение статей) — в облако (OpenRouter).
    *   **Контекст с памятью**: История диалога сохраняется в SQLite и подгружается автоматически.
    *   **Streaming**: Поддержка потоковой выдачи ответов (SSE) в API.
*   **💡 Рекомендательная система**: Подбор статей на основе содержимого вашей библиотеки и истории поиска.
*   **📄 Анализ PDF**: Инструменты для парсинга и извлечения информации из текстов статей.

## 🛠 Технологический стек

*   **Core**: Python 3.11+
*   **Telegram Bot**: Aiogram 3.x
*   **Backend API**: FastAPI (with SSE Support)
*   **Database**: SQLite (`aiosqlite`)
*   **LLM Infrastructure**: 
    *   **Ollama**: Локальный сервер инференса (модель qwen2.5:3b)
    *   **OpenRouter**: Облачный шлюз для GPT-4/Claude (суммаризация)
*   **Frontend**: HTML5, CSS3, JavaScript (Telegram WebApp)
*   **Containers**: Docker, Docker Compose

## ⚙️ Установка и настройка

### Предварительные требования
*   Python 3.11 или выше
*   Учетная запись Telegram и токен бота (от @BotFather)
*   Docker & Docker Compose (для запуска Ollama)

### 1. Клонирование и подготовка

```bash
git clone <url-репозитория>
cd AISA

# Запуск инфраструктуры (Ollama + DB)
docker-compose up -d
```

### 2. Установка зависимостей (для локальной разработки)

```bash
# Создание виртуального окружения
python -m venv .venv

# Активация и установка
# Windows: .venv\Scripts\activate
# Linux/macOS: source .venv/bin/activate
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=your_telegram_bot_token
WEBAPP_URL=https://your-domain.app 
OPENROUTER_API_KEY=sk-or-v1-...  # Для суммаризации
OLLAMA_BASE_URL=http://localhost:11434  # При запуске вне Docker
```

## 🚀 Запуск через Docker (Рекомендуется)

```bash
docker-compose up --build
```

## 🔌 API Chat Endpoints

Бот предоставляет API для интеграции чата (например, в Mini App):

*   `POST /api/v1/chat`: Синхронный ответ с результатами NLU.
*   `POST /api/v1/chat/stream`: Потоковая выдача (Server-Sent Events) чанков текста и результатов поиска.

**Пример стриминга:**
```bash
curl -N -X POST http://localhost:8000/api/v1/chat/stream/test \
  -H "Content-Type: application/json" \
  -d '{"message": "найди статьи по AI"}'
```

## 🚀 Запуск

Проект состоит из двух основных компонентов: Telegram-бота и API-сервера (для Mini App).

### Запуск Telegram бота
Основной процесс, обрабатывающий сообщения пользователя.

```bash
python main.py
```

### Запуск API сервера (Mini App Backend)
Сервер обслуживает веб-интерфейс и API запросы от Mini App.

```bash
# Используя uvicorn напрямую
uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload

# Или используя демонстрационный скрипт (для проверки)
python demo_launch.py
```

*Для локальной разработки Mini App рекомендуется использовать `ngrok` для проксирования локального сервера на HTTPS URL, который требует Telegram.*

```bash
ngrok http 8000
```

## 📂 Структура проекта

```
AISA/
├── api/                # FastAPI приложение (бэкенд для Mini App)
├── bot.py              # Инициализация бота
├── config/             # Конфигурационные файлы
├── database/           # Логика работы с БД (SQLite)
├── handlers/           # Обработчики команд и сообщений бота
├── middlewares/        # Middleware (логирование, авторизация)
├── nlp/                # NLP пайплайны (классификация, NER)
├── services/           # Внешние сервисы и бизнес-логика
│   ├── search/         # Адаптеры к научным базам (Arxiv, IEEE...)
│   ├── nlp/            # LLM сервисы
│   └── utils/          # Утилиты
├── webapp/             # Frontend часть (HTML/JS/CSS)
├── tests/              # Тесты (PyTest)
└── main.py             # Точка входа
```

## 🧪 Тестирование

Для запуска тестов используется `pytest`:

```bash
pytest
```
